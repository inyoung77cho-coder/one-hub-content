# S35 결과 — 모바일 글자 넘침 (EA·EB·EC)

> 실행 2026-09-13 · 지시서 `docs/S35_작업지시서.md` · 빌드 `next build --webpack` ✓ (Compiled successfully, exit 0)
> 순서: EA(지금 나는 넘침) → EB(재발 방지, ★본체) → EC(검증). 측정은 360px 뷰포트, 폰트 100%/137%.
> **목적은 16곳을 고치는 게 아니라 17번째가 안 생기게 만드는 것** → EB(유틸·fmt·감지기)가 핵심.

---

## 배치 EA — 지금 나는 넘침

### S35-1 · 반올림 누락 (앞선 실행에서 완료)

- `pages/pwa/today.js:370` `change_1d` → `.toFixed(1)` (`+10.018214936247727%` → `+10.0%`).
- 전수 확인 중 동일 버그 2곳: `pages/pwa/index.js:2055` `change_1d`, `:1813` `change_5d` → `.toFixed(1)`.

### S35-2 · `/pwa/etf` — 파일 / 처리 / 규칙

| 선택자 | 처리 | 파일 |
|---|---|---|
| `.oa-kind` "펀드" | **규칙 B** — `display:inline-block; max-width:100%; overflow:hidden; text-overflow:ellipsis; nowrap` 추가 | `pages/pwa/etf.js:1766` |
| `.me-qty` "222주 · 평단 204,918원" | **규칙 C** — `nowrap/ellipsis` → `white-space:normal; overflow-wrap:anywhere; word-break:keep-all; min-width:0` (수량·평단 두 줄 허용) | `pages/pwa/etf.js:1992` |
| `.an-vb-nm` "A133690" | **규칙 B** — 고정 `max-width:44px` → `max-width:100%`(칸 폭 사용) | `components/EtfAllocationPie.js:171` |
| `.an-vbars` +275px | **판정: 의도된 가로 스크롤 — 고치지 않음.** 아래 참조 | `components/EtfAllocationPie.js:66` |

**`.an-vbars` 는 의도된 가로 스크롤입니다.** JSX(66–67행)에서 `.an-vwrap { overflow-x:auto; -webkit-overflow-scrolling:touch }` 로 감싸고, 바 컨테이너에 `style={{minWidth: 종목수*46px}}` 를 명시해 **막대 차트를 좌우로 스크롤**하도록 설계돼 있습니다. 측정기의 `+275px` 는 스크롤 컨테이너 안쪽 콘텐츠 폭이라 **페이지를 밀지 않습니다**(부모 `.an-vwrap` 가 뷰포트 폭으로 클립). 지시서 지침대로 **그대로 두었습니다.**

`.oa-val`(금액)은 이미 `flex-shrink:0`, `.oa-name` 은 `min-width:0`+ellipsis 라 행이 페이지를 밀지 않음 — 확인 후 유지.

### S35-3 · 나머지 여섯 곳 — 파일 / 규칙

| 선택자(화면) | 규칙 | 처리 | 파일 |
|---|---|---|---|
| `.tds-reason` (today) | A+B | `overflow:hidden; text-overflow:ellipsis; min-width:0; max-width:100%` 추가 | `pages/pwa/today.js:1386` |
| `.story-fixed` (story) | A | ★원인 = `flex-shrink:0`(제목이 안 줄어듦) → `min-width:0`(제목이 줄며 줄바꿈, 버튼 `.story-change` 는 유지) | `pages/pwa/story.js:189` |
| `.md-arrow` (realestate) | A | 고정 `width:16px` → `min-width:1.3em; width:auto`(글자 크기 따라 늘어남 — 137%에서 화살표 잘림 해소) | `components/RePositionCard.js:849` |
| 퍼센트 셀 (assets) | A | 인라인 고정 `width:52/44` → `minWidth`(137%에서 "20.2%/75.3%" 잘림 해소) | `pages/pwa/assets.js:421-422` |
| `.ac-reason-k` (accuracy) | C | `nowrap/ellipsis/max-width:62%` → `white-space:normal; overflow-wrap:anywhere; word-break:keep-all; min-width:0` (규칙명 줄바꿈) | `pages/pwa/accuracy.js:270` |
| `.en-sub` (english) | C | `nowrap/ellipsis` → 줄바꿈 | `pages/pwa/english.js:969` |

> **규칙을 줄여서(글자 축소) 해결하지 않았습니다.** 상자가 늘어나거나 줄바꿈되게 고쳤습니다(큰 글씨 사용자 배려). `.story-change` 버튼은 지시서가 규칙 B로 표기했으나, **실제 넘침 원인은 옆의 제목(`.story-fixed`)이 안 줄어드는 것**이라 제목에 규칙 A를 적용했습니다(버튼에 말줄임을 걸면 "지역변경" 글자가 사라져 부적절).

---

## 배치 EB — 다시는 안 생기게 (★본체)

### S35-4 · 공용 유틸 클래스 — `styles/globals.css`

```css
.u-shrink { min-width: 0; }                                                          /* A */
.u-1line  { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 100%; }  /* B */
.u-wrap   { white-space: normal; overflow-wrap: anywhere; word-break: keep-all; }    /* C */
```
검증: CSS 번들(`.next/static/css/*.css`)에 `u-1line`/`u-wrap`/`u-shrink` 포함 확인 ✓.

### S35-5 · 숫자 포맷 일원화 — `lib/fmt.js` (신설)

`pct` / `signedPct` / `won` / `uk` / `qty` + 기존 `lib/format.js` 재export(창구 일원화).
**측정값(단위 테스트):**

| 호출 | 결과 |
|---|---|
| `pct(10.018214936247727)` | `"10.0"` ✓ |
| `signedPct(10.0182)` / `signedPct(-3)` | `"+10.0%"` / `"-3.0%"` ✓ |
| `won(1779934)` | `"1,779,934원"` ✓ |
| `uk(1073000000)` | `"10.73억"` ✓ |
| `qty(3000)` | `"3,000주"` ✓ |
| `pct(null)` / `won(NaN)` | `"0"` / `"0원"`(안전) ✓ |

규칙: 앞으로 화면에 나가는 숫자는 이 함수를 거친다(원시 숫자 직접 삽입 금지).

### S35-6 · 개발 중 자동 감지 ★재발 방지 핵심

- `pages/_app.js`: 개발 모드 전용 감지기(라우트 변경 1초 뒤 검사 → 넘친 요소에 `outline:2px solid red` + `console.table`).
  - ★프로덕션 제외: `if (process.env.NODE_ENV !== "development") return;` 상수 가드 → 빌드 시 DCE.
  - **검증: 프로덕션 번들(`.next/static`)에서 마커 `data-oh-of`·`[overflow]` 검색 = 0건 → 번들에 없음 확인 ✓.**
- `scripts/overflow-check.js`(신설): 프로덕션 콘솔 붙여넣기용 측정기. 큰 글씨 흉내 `document.documentElement.style.fontSize='22px'`.

### S35-7 · 전역 안전망 — 마지막에, 보험으로 (`styles/globals.css`)

```css
html, body, #__next { max-width: 100%; overflow-x: clip; }   /* ★hidden 아님 — sticky 안깨지게 */
```
EA·EB 완료 후 추가. 검증: CSS 번들에 `overflow-x:clip` 포함 ✓. **아래 EC에서 이 가드를 꺼도 통과함을 확인.**

---

## 배치 EC — 검증 (측정값)

**빌드**: `next build --webpack` ✓ Compiled successfully, exit 0.

**페이지폭 측정** (360px 뷰포트, dev 서버, 세션 발급해 게이트 통과. `pageW=documentElement.scrollWidth`):

| 화면 | 지시서 baseline (100%→137%) | 측정 후 100% (가드ON/OFF) | 측정 후 137% (가드ON/OFF) | 소수4자리+ |
|---|---|---|---|---|
| `/pwa/today` | 454 → 543 | **360 / 360** | **360 / 360** | 0 |
| `/pwa/story` | 354 → 428 | 360 / 360 | **360 / 360** | 0 |
| `/pwa` | 354 → 354 | 360 / 360 | 360 / 360 | 0 |
| `/pwa/accuracy` | 354 → 354(잘림1) | 360 / 360 | 360 / 360 | 0 |
| `/pwa/english` | 354 → 354(잘림1) | 360 / 360 | 360 / 360 | 0 |
| `/pwa/realestate` | 354 → 354(잘림4) | 360 / 360 | 360 / 360 | 0 |
| `/pwa/etf` | 354(넘침5/잘림4) | 360 / 360 | 360 / 360 | 0 |
| `/pwa/assets` | 354 → 354(잘림2) | 360 / 360 | 360 / 360 | 0 |
| `/pwa/settings` | 360 → 360(정상) | 360 / 360 | 360 / 360 | 0 |

- ★**가드 OFF(S35-7 무효화)에서도 9개 화면 모두 페이지폭 == 화면폭(360)** → 전역 가드가 증상을 가린 게 아니라 EA/EB로 실제 원인을 고친 것 확인.
- ★**소수점 4자리 이상 숫자 0건**(전 화면) — `[...document.querySelectorAll('*')].filter(e=>!e.children.length&&/\d+\.\d{4,}/.test(e.textContent)).length === 0`.
- position: sticky 헤더·하단탭: `overflow-x:clip`(hidden 아님) 사용으로 스크롤 컨테이너 미생성 → 정상.
- CRLF 유지·git diff 실변경만.

### ⚠️ 측정 범위의 한계 (정직하게)

이 환경은 **카카오 로그인 게이트 + 백엔드(:5001~5003) 미가동**이라, dev에서는 세션을 발급해 게이트는 통과했으나 **실데이터가 없어 대부분 화면이 셸(placeholder)로 렌더**됩니다. 따라서:

- 위 표의 "측정 후"는 **레이아웃/크롬 차원**의 넘침이 100%·137%(가드 OFF)에서 0임을 실측한 값입니다. today가 454→360으로 준 것은 데이터 의존 요소(15자리 숫자)를 코드에서 제거했기 때문이며, dev 셸에서도 360으로 측정됩니다.
- **데이터가 채워질 때만 나타나는 개별 요소 넘침**(`/pwa/etf` 의 `.me-qty`·`.oa-kind`, `/pwa/assets` 퍼센트 셀 등)은 규칙 A/B/C를 코드에 적용해 고쳤으나, **실데이터 육안 확인은 프로덕션에서 `scripts/overflow-check.js`(S35-6) 로 확인**하는 것이 정확합니다 — 그것이 이 스크립트를 만든 이유입니다.

---

## 남은 몫 (사용자 · 프로덕션)

1. 프로덕션 실계정에서 폰트 137%로 각 화면 + `scripts/overflow-check.js` 붙여넣기 → `넘침`/`잘림` 테이블이 비어야 완료. 특히 데이터가 많은 `/pwa/etf`·`/pwa/assets`.
2. 폰 실기기(아이폰/안드로이드) 큰 글씨 설정에서 육안.
3. 앞으로 새 화면: 글자 칸에 `u-shrink`/`u-1line`/`u-wrap`, 숫자는 `lib/fmt.js`, 개발 중 넘치면 빨간 테두리로 즉시 확인.
