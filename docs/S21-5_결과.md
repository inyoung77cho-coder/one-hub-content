# S21-5 결과 — 남은 중복 호출 → cachedJson

작업일 2026-09-01 · Claude Code

## 대상 (2차 점검 실측)
| 화면 | URL | 전 | 원인 |
|---|---|---|---|
| 오늘 | /api/pwa-dashboard | 2회 | today.js load() 재실행 |
| 오늘 | /api/today/news | 2회 | today.js + `<HoldingsNews>` |
| 전역 | /api/version | 2회 | EngineVersionBanner(_app) + FeedbackButton |
| AI | /api/index/history | 3회 | index.js effect deps(assetSum·fxRate) 재실행, 동일 URL |

## 한 것 (모두 `cachedJson` 경유 — 동일 URL 30초 dedup + in-flight 공유)
- `pages/pwa/today.js`: `pwa-dashboard`, `today/news` 를 `fetch().then(r=>r.json())` → `cachedJson()`.
- `components/HoldingsNews.js`: `today/news` → `cachedJson` (import 추가).
- `components/EngineVersionBanner.js`: `version` → `cachedJson` (import 추가). null(=실패) 시 기존 `.catch` 와 동일한 "unreachable" 상태로 수렴.
- `components/FeedbackButton.js`: `version` → `cachedJson` (import 추가).
- `pages/pwa/index.js`: `index/history` → `cachedJson`, null 방어(`d ? {...d} : {ok:false}`)로 기존 의미 보존.

## 안전성
- `cachedJson` 은 실패 시 `null` 반환(reject 아님) → 각 호출처 then 본문이 null 을 견디는지 개별 확인(모두 `d?.` 가드 또는 명시 분기).
- 스코프 밖(blog·dashboard·market-center 등)은 미변경.

## 검증
- `bash scripts/verify_s19.sh` → **FAIL=0**
- `npm run build`(webpack) → **Compiled successfully**
- 합격선(두 화면 API ≤30회·동일 URL 중복 0건): 배포 착륙 후 로그인 콘솔에서 `performance.getEntriesByType('resource')` 로 확인(사용자).

## 측정 스니펫 (배포 후)
```js
const es = performance.getEntriesByType('resource').filter(e=>e.name.includes('/api/'));
const dup = {}; es.forEach(e=>{const u=e.name.replace(location.origin,'');dup[u]=(dup[u]||0)+1;});
console.log('calls', es.length, '중복', Object.entries(dup).filter(([,n])=>n>1));
```
