# S21-3 결과 — 오늘 탭 제목 "오늘의 대결" → "오늘의 자산"

작업일 2026-09-01 · Claude Code

## 원인
S20-3 에서 대결 카드는 오늘 탭에서 제거했는데, `pages/pwa/today.js` 의 `RotatingPageTitle` 첫 항목이 여전히 `{ suffix: "의 대결" }` → 제목 "오늘의 대결"이 첫 화면 내용(총자산·오늘 조치·AI 변화)과 불일치.

## 한 것 (`pages/pwa/today.js` 1줄)
`items` 첫 항목을 `{ suffix: "의 자산" }` 으로 변경. 나머지(부동산/ETF/이야기)와 "종목변경" 순환 동작은 그대로.

## 검증
- `bash scripts/verify_s19.sh` → **FAIL=0**.
- `npm run build`(webpack) → **통과**.
- 합격선: 오늘 탭 제목이 "오늘의 자산"으로 표시(로그인 브라우저 확인).
