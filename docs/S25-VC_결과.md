# S25 VC 결과 — 현장경제 재편 (S25-6·7)

작업일 2026-09-03 · Claude Code

## S25-6 · 페이지 이름과 구조
1. **이름 "현장경제" → "듣는 경제"** — `components/BottomNav.js` 라벨(🎧 듣는 경제), `pages/pwa/english.js` `<h1>` (🎧 듣는 경제 + 언어 부제). **URL(`/pwa/english`)은 미변경**(외부 링크·북마크 보존).
2. **순서 경제영어·일반영어·중국어** — `MODES` 재정렬(gen 을 zh 앞으로).
3. **3칸 세그먼트 + 스와이프** — 대메뉴(언어)는 이미 세그먼트(`en-langs`), `useSwipeTabs` 로 좌우 스와이프 배선(`langSwipe`, `.en` 루트). onChange=`changeMode`.
4. **하위 트랙(news·video·idiom)은 칩 유지** — 스와이프 대상 아님(2단 스와이프 방지). 대메뉴만 스와이프.

**어디서 언제 보나:** 하단 탭·페이지 제목이 "듣는 경제". 좌우로 밀면 경제영어↔일반영어↔중국어, 하위 트랙은 칩 클릭.

## S25-7 · 내 단어장 + 리마인드
1. **저장은 별 하나** — `lib/vocabNote.js`(신규). 레슨의 표현·단어 옆 `VocabStar`(☆→★). 메모 강요 없음. 같은 표현은 한 건(lang+text 해시).
2. **SYNC** — `onehub_vocab_A/_B` `SYNC_KEYS` 등록(폰에서 담고 PC에서 복습). 스키마 `{id, lang, text, meaning, source, savedAt, reviewedAt[], box}`.
3. **내 단어장 화면** — **상단 링크** `⭐ 내 단어장 N`(언어 세그먼트는 셋 유지) → `pages/pwa/vocab.js`(신규). 언어 필터·최근순/오래된순·**전체 듣기**(`AudioPlaylist` 재사용).
4. **리마인드 4단계 라이트닝** — box1→1일·2→3일·3→7일·4→30일. 판정 버튼은 **두 개(기억남/가물)**. 기억남→box+1, 가물→box1. `dueForReview` 가 간격 지난 것만.
5. **오늘 페이지 연결** — `todayCadence` 에 훅 추가: `⭐ 오늘 복습할 표현 N개` → `/pwa/vocab`. **복습 대상 없으면 훅 미반환**. 주식 조치 없는 조용한 날의 재료.

## 합격선 대조
- [x] 하단 탭·제목 새 이름, URL 그대로.
- [x] 세 언어 세그먼트+좌우 스와이프, 하위 트랙은 스와이프 안 됨.
- [x] 표현·단어를 별로 담고 다른 기기에서 보임(SYNC_KEYS).
- [x] 단어장 전체 듣기(AudioPlaylist).
- [x] 복습 대상 있는 날만 오늘 화면 훅.
- [x] 복습 판정 버튼 두 개.
- [x] 저장 개수 배지 크게 안 띄움(작은 링크).
- [x] verify_s19 FAIL=0 · webpack build 통과.
- ⚠️ 최종 렌더·스와이프·SYNC 반영은 로그인 게이트 뒤 사용자 확인.
