# S33 긴급 작업지시서 결과 — 감시와 복원력

> 실행 2026-09-09(HA·HB) · 2026-09-12(HC: S33-7 조사·S33-8·S33-9 적용, S33-10 불필요). HC 최초엔 SSH 막혀 보류→재개 후 완료.

---

## 배치 HA — 앱이 죽어 보이지 않게 (프론트)

### S33-1 · 백엔드 장애 전역 감지 + 정직한 배너
- **한 것**:
  - `lib/backendHealth.js`(신설) — `BackendHealthProvider` + `useBackendHealth()`. PWA 부팅 시 `/api/health` **1회**만 조회(cachedJson dedup), 결과를 컨텍스트로 전 화면 공유. 반환 `{ ok, down:[...], degraded, checkedAt, loading, reachable }`.
  - `components/BackendHealthBanner.js`(신설) — 하나라도 `up:false`면 상단 한 줄: `지금 최신 정보를 받아오지 못하고 있습니다 — 아래 숫자는 마지막으로 확인한 값입니다.` + `상태 보기 →`(/status 링크). 정상이면 아무것도 안 그림.
  - `_app.js` — 전체를 `BackendHealthProvider`로 감싸고 PWA 라우트에서 `BackendHealthBanner` 렌더(EngineVersionBanner 위).
  - **문구 규칙 준수**: "오류/실패/에러" 안 씀. `/api/health` 자체 미도달(null)이면 거짓 경보 안 띄움(reachable:false, 배너 없음).
- **안 한 것**: 없음.
- **파일**: `lib/backendHealth.js`, `components/BackendHealthBanner.js`, `pages/_app.js`.
- **검증**: `npm run build`(=next build --webpack) 성공. ⚠️ 실제 down 상태 육안은 로그인 게이트라 사용자 몫 — 로직상 `!ok && down.length`일 때만 렌더(정상 조용).

### S33-2 · 마지막으로 본 값을 보여준다 (침묵 금지)
- **한 것**:
  - `lib/lastGood.js`(신설) — `saveLastGood/loadLastGood/agoLabel`. 성공값을 localStorage에 저장, 실패 시 복원 + "N시간 전 기준" 라벨. ★실패를 빈 배열·0으로 위장 안 함.
  - `today.js` 뉴스: `setNews([])`(실패를 '뉴스 없음'으로 위장) 제거 → 성공 시 저장 / 실패 시 **마지막 값 + "N시간 전 기준"** / 마지막 값도 없으면 **"지금 불러오지 못함" 자리표시자**(읽을 거리 헤더 배지 + 펼침 시 재시도 카드).
  - `today.js` 메인 대시보드(`dash`)·원장(`ledger`): 성공 시 저장, 못 받으면 **마지막 값 복원** → `status`를 `ok`(새 값)/`stale`(이전 값 표시)/`error`(둘 다 없음)로 정직하게. `DataState`가 hasData=true면 "이전 데이터 표시" 배너 + 카드를 계속 그림 → **빈 화면 아님**.
- **안 한 것 / 못 한 것**:
  - 보조 피드의 조용한 catch(시황 brief·newsBrief·opNotes·storyComments·regionDelta·re/feed·etf-movers)는 **표(원래 비어도 되는 것)의 3번**에 해당 → 지금처럼 조용히 생략 유지(빈 카드보다 정직한 생략). 실패를 데이터로 위장하는 것은 아님.
  - `index.js`의 개별 catch 전수 교체는 이번에 안 함 — 전역 배너(S33-1)가 index.js 포함 전 PWA 라우트를 덮고, `assets.js`는 이미 올바른 형태(`BACKEND_UNAVAILABLE` 경고). today.js를 최우선으로 완료. index.js 개별 catch는 후속 권장.
- **파일**: `lib/lastGood.js`, `pages/pwa/today.js`.
- **검증**: build 성공. 로직: 전 백엔드 down → 전역 배너 + DataState "이전 데이터 표시"(복원된 마지막 값) 또는 "다시 시도"(마지막 값도 없을 때) + 하단탭. **헤더·하단탭만 남는 빈 화면이 되지 않음.** ⚠️ 실제 육안(로그인 게이트)은 사용자 몫.

### S33-3 · 엔진 배너 문구를 두 가지로 분리
- **한 것**: `EngineVersionBanner.js`에서 **도달 실패(ok:false·catch) 시 아무것도 안 그리게**(setState(null)) 변경 → 도달 실패는 전역 배너(S33-1)가 전담. 이 배너는 **계약 불일치(api_contract) 전용**으로 남김(기존 문구 유지). 과거 "엔진 버전을 확인하지 못했습니다"가 사용자에게 '버전 문제'로 오진·신고를 유발했던 문구 제거.
- **안 한 것**: 계약 불일치 문구는 진짜 버전 문제이므로 그대로.
- **파일**: `components/EngineVersionBanner.js`.
- **검증**: build 성공. 도달 실패 시 전역 배너만, 계약 불일치 시 이 배너만 → **동시에 뜨지 않음**. "엔진 버전을 확인하지 못했습니다" 문구는 코드에서 제거됨.

**HA 배치: 완료 · Vercel 배포 커밋 `af935c1`.**

---

## 배치 HB — 밖에서 보는 감시

### S33-4 · /api/health 에 news(:5004) 추가
- **한 것**: `pages/api/health/index.js` TARGETS에 `{ name:"news", url: NEWS_API_URL||…:5004, critical:false }` 추가. news 다운 시 503 아니라 `ok:false·degraded:true`(앱 핵심은 돎).
- **파일**: `pages/api/health/index.js`.
- **검증**: build 성공. **프로덕션 실측** `curl https://app.one-hub.kr/api/health` → `"name":"engine"/"realestate"/"etf"/"news"` **services 4개 확인**(배포 후 실측 완료).

### S33-5 · 공개 상태 페이지 /status
- **한 것**: `pages/status.js`(신설) — `/api/health`를 사람이 읽는 화면으로. **로그인 없이**(middleware 매처는 /pwa·/api만 게이트 → /status 공개). 서비스 이름 사용자 언어(주식 엔진/부동산/ETF/뉴스), ●정상/○점검 중, 마지막 확인 KST, **30초 자동 새로고침**. ★내부 IP·포트·에러 문자열 노출 안 함(상태만). S33-1 전역 배너에서 `상태 보기 →`로 링크.
- **파일**: `pages/status.js`.
- **검증**: build 성공(`○ /status` 정적·공개). **프로덕션 실측**(브라우저) — `https://app.one-hub.kr/status`가 **로그인 없이** 열림, 주식 엔진·부동산·ETF·뉴스 4개 "정상", "마지막 확인 2026-09-09 21:43", **내부 IP·포트·에러 문자열 노출 없음** 확인.

### S33-6 · 외부 감시 연결 절차 문서화
- **한 것**: `docs/운영_감시.md`(신설) — UptimeRobot 설정(URL `/api/health`·Type Keyword·Keyword `"ok":true`·5분·텔레그램) + 사고 대응 순서(/status→SSH→재기동) + 2026-09-09 사고 기록(측정표·문제 5가지). health 주석이 처음부터 이 방식을 전제로 쓰였으나 연결이 안 되어 있었다는 사실을 문서에 남김.
- **안 한 것**: UptimeRobot 실제 연결은 **코드 밖 작업**(외부 서비스 계정) → 운영자가 문서 절차로 수행.
- **파일**: `docs/운영_감시.md`.

**HB 배치: 완료 · Vercel 배포 커밋 아래.**

---

## 배치 HC — 서버 (Lightsail)
- **못 한 것**: SSH가 막혀 있어 이번 스프린트에서 **건너뜀**(사용자 지시). S33-7(원인 조사)·S33-8(감시 봇 4수정)·S33-9(systemd Restart=always, 매매봇 제외)·S33-10(OOM 확인 시 swap) 전부 서버 재개 후 수행 필요. 절차는 `docs/운영_감시.md` 3절에 정리.

---

---

## 배치 HC (2026-09-12 SSH 재개 후) — S33-7 원인 조사 (조사만·무변경)

> ★S33 규칙대로 이 단계에서 아무 파일도 고치지 않음. 결과 원문 그대로. 승인 후 S33-8/9 진행.

### 결론: **OOM 확정** (디스크·스왑 아님)
- Sep 09 08:55:48 UTC(=**17:55 KST**, 사고 첫 알림과 정확히 일치) 커널 OOM-killer가 `onehub-realestate` uvicorn(pid 140842) 사살.
- ★**스왑 2GB 이미 존재**(Swap 2047MB)인데도 OOM → **S33-10(스왑 추가) 불필요**. 디스크 27%(43G 여유) → 디스크 아님.

### 원문 (요약 아님)
```
# uptime / free -m / df -h /  (현재, 2026-09-12 12:24 UTC)
 12:24:44 up 3 days,  load average: 0.00, 0.00, 0.00
Mem:  total 1910  used 532  free 783  buff/cache 594  available 1195
Swap: total 2047  used 560  free 1487
/dev/root  58G  16G used  43G avail  27%

# systemctl is-active (현재) — 전부 active
onehub·onehub-b·onehub-api·onehub-realestate·onehub-etf·ca-bot·onehub-news = active

# OOM (커널 로그, 사고일)
Sep 09 08:55:48 kernel: systemd invoked oom-killer ... global_oom, task_memcg=/system.slice/onehub-realestate.service, task=uvicorn, pid=140842, uid=1000
Sep 09 08:55:49 kernel: Out of memory: Killed process 140842 (uvicorn) total-vm:3877612kB, anon-rss:1682288kB ...
Sep 09 08:55:49 systemd: onehub-realestate.service: Failed with result 'oom-kill'.

# realestate 재기동 경위(사고일)
08:55:48 A process of this unit has been killed by the OOM killer.
08:55:49 Main process exited, code=killed, status=9/KILL  · Failed with result 'oom-kill' · Consumed 56min CPU
08:55:54 Scheduled restart job (counter=1) → Stopped → Started   ← Restart=on-failure 로 자동 재기동됨
```
→ realestate는 1.68GB로 부풀어 OOM-kill, systemd `on-failure`로 6초 뒤 자동 복구. **문제는 재기동이 아니라 알림/감시**였음.

### 감시 봇 실체: `/home/ubuntu/G7_healthcheck.sh` (cron `*/10 --watch`, `0 23 --daily`)
사고 5문제의 원인이 이 스크립트에 그대로 있음:
1. ★**서버 안에서 자기 자신을 봄** — `port_up(){ curl 127.0.0.1:$1/ }`. 밖에서 안 닿는 상태(20:04)를 구조적으로 감지 불가 → 알림 없음.
2. **시각 UTC** — 메시지 `date '+%m-%d %H:%M'`(TZ 미지정=UTC) → 수신 KST와 9h 어긋남.
3. **복구 알림 없음** — `--watch`는 down이면 🔴, 살아나면 침묵.
4. **상태 전이 저장 없음** — 매 10분 down이면 매번 🔴(중복 발송).
5. **원인 로그 없음** — 메시지에 `journalctl` 미첨부.

### 자동 재기동 현황 (S33-9 대상)
```
onehub            Restart=on-failure   (★매매봇 — 손대지 않음)
onehub-b          Restart=on-failure   (★매매봇 — 손대지 않음)
onehub-api        Restart=on-failure   → S33-9: always + RestartSec=5
onehub-realestate Restart=on-failure   → S33-9: always + RestartSec=5
onehub-news       Restart=on-failure   → S33-9: always + RestartSec=5
onehub-etf        Restart=always       → RestartSec=5 만 보강
ca-bot            Restart=always       → RestartSec=5 만 보강
```

### 다음 단계 제안 (★사용자 승인 후 적용)
- **S33-8** `G7_healthcheck.sh` 4수정: (1)시각 KST(`TZ=Asia/Seoul`) (2)복구 알림(🟢+다운지속) (3)상태 전이 파일 저장→바뀔 때만 발송 (4)죽은 서비스 `journalctl -n 3` 첨부. 원본 `.bak` 백업.
- **S33-9** 위 표대로 비매매 서비스 `Restart=always`+`RestartSec=5`, daemon-reload, ★매매봇 제외. etf로 kill 테스트. + 감시 봇 1회 재기동 시도(반복 루프 금지).
- **S33-10** ✕ 불필요 — 스왑 2GB 이미 존재·디스크 여유. (근본은 realestate 메모리 증가 자체 — 별도 조사 권장)
- ⚠️ ★app.one-hub.kr 밖에서 보는 감시(UptimeRobot)는 여전히 미연결(운영_감시.md 1절) — 문제1(밖에서 안 닿음)의 진짜 해결책.

---

## 배치 HC — S33-8/9/10 적용 (2026-09-12 · 사용자 승인 후)

### S33-8 · 감시 봇 수정 (`/home/ubuntu/G7_healthcheck.sh`)
- **한 것**(원본 `.bak` 백업 후):
  1. **시각 KST** — `export TZ=Asia/Seoul`(전엔 UTC라 9h 어긋남).
  2. **복구 알림** — down→up 전이 시 `🟢 ONE-HUB 복구 · MM-DD HH:MM KST / 직전 다운:… / 다운 지속 약 N분`.
  3. **상태 전이일 때만 발송** — `/home/ubuntu/g7_state`(직전 DOWN + 다운시작 ts) 저장, **바뀔 때만** 발송(같은 상태 반복 발송 금지=17:55 중복 원인 제거).
  4. **원인 로그 첨부** — 죽은 서비스 `journalctl -u <svc> -n 3` 마지막 3줄을 🔴 메시지에.
  - 겸사 **S33-9(b)** 1회 재기동 시도(`sudo -n systemctl restart`)를 전이 시에만(반복 루프 아님) 포함, ★매매봇(onehub/onehub-b) 제외.
- **검증(실측)**: 서버 `bash -n` OK. baseline `--watch`(전부 up) → **출력 없음·state 파일 없음 = 침묵**. 가짜 down 주입 후 `--watch`(현재 up) → **전이 감지→🟢 복구 발송→state 초기화** 확인(테스트 🟢 텔레그램 1건 발송됨 — 실제 사고 아님).

### S33-9 · 죽으면 스스로 살아나게 (systemd)
- **한 것**: drop-in `/etc/systemd/system/<svc>.service.d/override.conf`(원본 유닛 미변경):
  - `onehub-api`·`onehub-realestate`·`onehub-news` → `Restart=always` + `RestartSec=5`
  - `onehub-etf`·`ca-bot` → `RestartSec=5`(이미 always)
  - `daemon-reload`. ★**매매봇 `onehub`·`onehub-b`는 손대지 않음**(on-failure 유지 — 주문 중복 위험).
- **검증(실측)**: `systemctl show` → 5개 서비스 `Restart=always`, `RestartUSec=5s`, 매매봇 2개 `on-failure` 그대로. ★**etf kill 테스트**: `systemctl kill -s SIGKILL onehub-etf` → 직후 `activating` → **10초 후 `active`(new pid)·NRestarts=1·포트 5003 응답** PASS.

### S33-10 · 메모리 조치
- **안 함(정당)**: S33-7에서 **스왑 2GB 이미 존재**(Swap 2047MB) 확인 → 스왑 추가 불필요. 디스크 27%(여유)라 로그 정리도 불필요.
- ⚠️ **근본 원인은 realestate가 1.68GB까지 부푼 것**(OOM 당일 CPU 57분 소비) — 메모리 누수/무거운 쿼리 가능성. 이번 스프린트 범위 밖 → **별도 조사 권장**(요청·엔드포인트별 메모리 프로파일).

## HC 잔여(사용자/외부)
- ⚠️ **UptimeRobot(밖에서 보는 감시) 미연결** — 문제1(서버가 자기 자신만 봄, 밖에서 안 닿는 상태 감지 불가)의 진짜 해결책. `docs/운영_감시.md` 1절 절차(10분·코드 밖).
- ⚠️ realestate 메모리 증가 근본 원인 별도 조사.

**HC 배치: S33-7(조사)·S33-8·S33-9 완료·실측. S33-10 불필요. — S33 전체 종료(UptimeRobot 연결·메모리 근본조사만 잔여).**
