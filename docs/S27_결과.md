# S27 결과 — 서버 작업 2건 (일별 시세 수집 · WG 테마클립 엔드포인트)

> 2026-09-04 · S26 스프린트가 S27로 분리했던 서버 작업. Lightsail 54.180.54.132 배포·검증 완료.

## 1) ETF 일별 종가 수집기 (:5003 onehub-etf)
- **근본 원인**: S25-8 `/api/etf/history` 가 stooq 일별(/q/d/l/)을 쳤으나 AWS/Vercel 에서 robots noindex 로 차단(빈 응답) → `EtfTrendTheme` 곡선이 계속 "이력 수집 중".
- **해결**: 서버에서 **FinanceDataReader**(auto_trade venv, fdr 0.9.202)로 수집. 미국(QQQM·SCHD…)·KRX(133690) 모두 정상 확인.
- **배포물**:
  - `/home/ubuntu/one-hub/etf/history_collector.py`(신규) — `etf_daily(ticker,date,close,updated)` 테이블 ensure/upsert, `get_history`(오늘자 있으면 DB, 없으면 수집), `refresh_all`(보유+기존 티커). A접두사(A133690→133690) 정규화.
  - `service.py` 에 `@app.get("/api/etf/history")` — `{ok, ticker, closes:[{date,close}]}`. before_request X-API-Key 보호 그대로.
  - 크론: `50 21 * * 1-5`(영업일, 기존 KIS/domestic 수집기 뒤) `timeout -k 60 300 … history_collector.py refresh_all`.
- **검증**: `refresh_all` → 12/12 저장. `get QQQM` → 40포인트. `curl localhost:5003/api/etf/history?ticker=QQQM` → ok=True, last 295.51.
- **프론트**: `pages/api/etf/history.js` 를 stooq 직접 → **:5003 프록시**(ETF_API_URL/ETF_API_KEY, 기존 ETF 프록시와 동일 env·CDN 하루1회). 커밋 `6076e5e`.

## 2) WG 테마클립 엔드포인트 (:5005 onehub-english)
- `app/api/lessons.py` 에 `@router.get("/theme-clip")` 추가 — `track`·`language`·`level`.
  - 오늘(없으면 최근) 해당 테마·언어의 published 레슨 제목을 모아 **목표어 브리지 나레이션**(intro/bridges/outro/outro_ko) 생성.
  - **비용 서킷브레이커**: `llm.chat_json`(내부 `claude_budget.guard`+`log_usage`) 경유. 별도 우회 없음. prefer=claude-sonnet-5, max_tokens 1500.
  - **캐시**: `data/theme_clip/{lang}_{track}_{date}.json` — **테마·언어당 하루 1회** 생성, 이후 파일 캐시(재시작 견딤).
  - 실패(레슨 없음·LLM 불가·생성 실패)면 `{ok:false}` → 프론트가 브리지 없이 레슨만 재생.
- **검증**: `economy/en` 1차 ok=True(intro·bridges 2·한국어 요약), 2차 **1.8s 캐시(LLM 미호출)**.
- 프론트(S26 WG `eb55021`)가 이미 이 엔드포인트를 호출·조립(`assembleThemeClip`) → 이제 실제 목표어 해설이 붙는다.

## 배포 안전 절차(공통)
서버 원본 새로 scp → 로컬 편집 → `venv py_compile` → 백업(`.bak_$TS`) → scp → `systemctl restart` → `is-active` 확인 → 로컬/외부 curl 검증. 한 서비스씩.

## 남은 것 / ⚠️ 사용자 확인
- **비용 모니터링**: theme-clip 은 조합(6)당 하루 1회 LLM. 실사용 시 `console.anthropic.com` 및 `data/claude_usage_english.db` 로 실제 비용 대조 권장(요금표는 추정치).
- **user_state 실계정 end-to-end 검증 + 동기화 실패 화면 노출**(S27 §의 나머지) — 미착수. 별도 진행 필요.
- daily 클립(S25-10) ⑥ 외국어 구간을 theme-clip 으로 완전 병합(sync→async) — 후속.
- EtfTrendTheme 실제 곡선 채워짐은 배포 후 폰(로그인)에서 육안 확인 권장.
