# S17-0 Part 2 복원 시나리오 판정

작성 2026-07-17 · 서버 `ip-172-26-3-68` · 소스 6곳 전수 탐색 완료 · **코드 복원 0건**

---

## 판정: 【제4의 시나리오 — 복원 불필요】

지시서 W2-0이 제시한 3개(완전복원 / 부분복원 / 복원불가) **어디에도 해당하지 않습니다.**
**롤백이 일어난 적이 없기 때문입니다.**

| 항목 | 판정 |
|---|---|
| 시나리오 | **복원 불필요** — 되돌릴 것이 없음 |
| 롤백 시점 | **없음** |
| 롤백 원인 | **롤백 자체가 발생하지 않음** |
| 후퇴한 파일 | **0개** |
| 살아 있는 v9.x 기능 | **13/13 전부** (W2-4 체크리스트 전량) |
| v9.x 소스 확보처 | ① `.bak` — v9.1 실물 3개 확보(다만 복원 대상 아님) |
| 복원 가능 기능 | **해당 없음** (복원할 대상이 없음) |
| 재구현 필요 기능 | **없음** |
| 예상 소요 | **0** — Part 2 종료 |

---

## 근거 1: 버전은 후퇴한 적이 없고 단조 증가했습니다

`.bak` 20개의 `APP_VERSION`을 mtime 순으로 정렬한 실측 이력입니다.

| 시각 | APP_VERSION | 파일 |
|---|---|---|
| 06-15 12:35 | 8.0 | `main.py.bak_20260615_123505` |
| 06-17 13:20 | 8.0 | `main.py.bak_` |
| **06-19 13:00** | **8.2** | `main.py.bak_v82` ← **지시서 F1이 "현재 배포본"이라 한 버전** |
| 06-19 14:03 | 8.3 | `main.py.bak_v83` |
| 06-20 12:24 | 8.4 | `main.py.bak_v8.5_killbug` |
| 07-02 13:15 | 8.7 | `main.py.bak_v90_20260702_221518` |
| **07-03 12:22** | **9.1** | `main.py.bak_v93bug02_20260703_131143` |
| 07-07 13:34 | 9.1 | `main.py.bak_queue_20260707_133407` |
| 07-07 13:55 | 9.1 | `main.py.bak_ver_20260707_135509` ← v10 전환 직전 |
| **07-07 13:55** | **v10.0.0-ops** | `version.py` (현재 가동본) |

**v8.2는 2026-06-19의 스냅샷입니다.** 지시서 F1은 한 달 전 기록을 현재로 오인했습니다.

전환 스크립트도 실물로 남아 있습니다 — `patch_v91_engine.py:19`:
```python
apply('APP_VERSION = "8.7"', 'APP_VERSION = "9.1"', 1)
```

봇 기동 메시지(journalctl, 저널 보존 7/3~)도 일치합니다:
```
5회  ONE-HUB AI Auto Trading v9.1
2회  ONE-HUB AI Auto Trading v10.0.0-ops
```
**v9.1 → v10.0.0-ops 방향으로만 이동했습니다. 역방향 메시지는 0건입니다.**

---

## 근거 2: v9.1 → v10 변경분은 56줄이고, 기능 삭제가 없습니다

`diff -u main.py.bak_ver_20260707_135509 main.py` (= v9.1 마지막 스냅샷 vs 현재)
→ `/tmp/v9_vs_v10_diff.txt` (56줄). 핵심은 버전 상수 단일화뿐입니다.

```diff
-APP_VERSION = "9.1"
-APP_VERSION_STR = f"v{APP_VERSION}"
+from version import APP_VERSION as APP_VERSION_STR   # [TASK-0] 단일 소스 "v10.0.0-ops"
+APP_VERSION = APP_VERSION_STR.lstrip("v")            # f"v{APP_VERSION}" 호환용
```

**S-4(APP_VERSION 불일치)의 실체가 이것입니다.** 불일치는 코드가 아니라 **문서**에 있습니다.

---

## 근거 3: W2-4 13개 기능 — 전부 생존 (유실 0)

| # | 기능 | 현재 존재? | 복원 필요 | 실측 근거 |
|---|---|---|---|---|
| 1 | STOCK_POOL 92종목 | ✅ **182종목** | ✗ | `stock_screener.py:33`, 292행 `_deduped`. **기대치보다 많음** |
| 2 | 방향성 스코어링(방산/헤지/인버스 BEAR 예외) | ✅ | ✗ | `stock_screener.py:420~435` `direction in ("hedge","inverse")` / `"defense"` / `"defensive"` |
| 3 | `get_stock_regime()` | ✅ | ✗ | `stock_screener.py:418` |
| 4 | Block Accuracy Engine | ✅ | ✗ | 코드 존재 · `block_accuracy` 38행(단 **6/17 이후 기록 중단** — 별도 이슈) |
| 5 | KIS 서킷브레이커 + 토큰관리 | ✅ | ✗ | `kis_api.py:48` **v9.1 [ENG-01]**, 임계 5회·쿨다운 900s |
| 6 | KST 타임존 수정 | ✅ | ✗ | UTC 23:50 = 08:50 KST 정시 실행 확인 |
| 7 | PWA Web Push | ✅ | ✗ | `engine_status_api.py` `/api/push/vapid-key` 외 |
| 8 | 카카오톡 연동 | ✅ | ✗ | `main.py` `kakao_refresh` 스케줄(5시간) |
| 9 | `event_log` 통합 테이블 | ✅ | ✗ | 테이블 존재 |
| 10 | weekly "차단신호 Top3" | ✅ | ✗ | `weekly_generator.py` |
| 11 | 텔레그램 `/aimode` | ✅ | ✗ | `main.py` 존재. **S-2의 "사라진 명령어"는 사라지지 않았음** |
| 12 | ML 스코어링 | ✅ | ✗ | `ml_score` 컬럼 + `ML_STRONG_SELL` 신호 |
| 13 | A/B 트레이더 분리 | ✅ | 유지 | `Environment=TRADER_ID=A`(onehub) / `B`(onehub-b) |

**13/13 생존. 복원 대상 0건.**

---

## 근거 4: 나머지 소스 4곳도 전부 "유실 없음"을 가리킵니다

| 소스 | 결과 |
|---|---|
| ② vscode History | **auto_trade 단서 0건.** 전체 20K·파일 2개이며, 내용은 **부동산 엔진**(`re_complex_endpoints.py`, 7/11) 세대 1개뿐 |
| ③ 로컬 `C:\onehub` | 별도 v9.x 사본 없음. 프론트(`one-hub-content`)만 git 관리 |
| ④ 텔레그램/journalctl | v9.1 → v10.0.0-ops **전진만**. v8.2를 말한 메시지 **0건**(저널 보존 7/3~) |
| ⑤ DB 스키마 화석 | 코드가 참조하지 않는 테이블 = `sqlite_sequence`(SQLite 내부)뿐 → **코드와 DB 완전 일치**. v9.x가 남긴 고아 테이블 없음 |
| ⑥ PWA 호출 엔드포인트 | `/api/assets/total`이 엔진에 부재 — 단 이는 **구현된 적 없는 계약**이지 유실이 아님(Part 0에서 확인, 프론트에서 2차 소스로 우회 완료) |

---

## S-2 재판정: "텔레그램 버전 후퇴"는 무엇이었나

- 배포본·가동 프로세스 모두 **v10.0.0-ops**
- 텔레그램이 출력하는 문자열 = `f"🤖 AI 자율 운영 현황 (v{APP_VERSION})"` → **v10.0.0-ops**
- `/aimode` 포함 명령어 **생존**
- 기동 메시지 이력에 **후퇴 방향 0건**

**S-2는 재현되지 않습니다.** 다만 사용자가 관측한 "이상함"은 실재했고, 그 실체는 버전이 아니라
**2026-06-30 이후 매수 0건**(Part 1 판정: 승인→집행 체인 단절)일 가능성이 높습니다.
텔레그램으로 오던 승인 요청이 응답 없이 만료되면서 **봇이 "일을 안 하는 것처럼" 보였을 것**입니다.

---

## W2-7 복원 실행: 수행하지 않음

**의도적으로 건너뜁니다.** 실행하면 다음이 됩니다:

```
현재 v10.0.0-ops  ──(지시서대로 "v9.x 복원")──▶  v9.1
                    = 신버전을 구버전으로 덮는 실제 롤백
```

지시서 0-2가 경고한 사고 — *"잘못된 SCP 업로드 한 번으로 코드가 조용히 되돌아가고, 되돌아간
사실조차 감지되지 않는다"* — 를 **지시서를 따르면 우리 손으로 일으키게 됩니다.**

W2-9 체크리스트 중 복원 관련 항목(#18 선별복원 / #20 APP_VERSION 갱신 / #21 STOCK_POOL 92종목
복원 / #22 방향성 예외 복원 / #23 `/aimode` 복원 / #25~26 드라이런)은 **복원 대상이 없으므로
"해당 없음"**입니다. 특히 #21은 **이행하면 182종목을 92종목으로 축소하는 기능 후퇴**가 됩니다.

---

## 안전규칙 이행

| 규칙 | 이행 |
|---|---|
| 1. 복원 전 전체 스냅샷 | ✅ 서버 `/home/ubuntu/snapshot_20260717_1455` (1.3G, auto_trade + trading.db `.backup`) + 로컬 `C:\onehub\snapshots\snapshot_20260717_1455_src.tgz` (1.9M, 소스 40개) **2벌** |
| 2. 암호화 키·토큰 미변경 | ✅ `crypto_utils.py`(mtime 06-12) · `kis_token_*.json` **손대지 않음**. ★ `.env` mtime 이 오늘로 찍히는데 **우리가 쓴 게 아니라 봇이 재시작 시 `[KAKAO_REFRESH] access_token updated` 로 스스로 갱신**한 것(정상 동작). → `.env` mtime 으로 '사람이 건드렸나'를 판단하면 재시작마다 오탐. 검증 스크립트에서 교정함 |
| 3. Trader B 무변경 | ✅ B 데이터 미접근. `onehub-b`는 재시작만(코드 공유) |
| 4. 장중 배포 금지 | ✅ **오늘 공휴일 휴장**(사용자 확인) + 배포 스크립트가 `trades` 오늘 체결 0건으로 교차검증 |
| 5. 드라이런 1거래일 | 해당 없음(복원 미실시). **단 오늘 배포한 관측성 3건은 다음 거래일 09:00 신호에서 자동 검증됨** |
| 6. `.bak` 미삭제 | ✅ 64개 + 오늘 추가된 `*.bak_s17e_20260717_055235` 3개 전량 보존 |

---

## Part 3으로 넘기는 사항

| # | 내용 |
|---|---|
| 1 | **롤백 원인 규명(W2-9 #9)은 "원인 없음"** — 롤백이 없었으므로 Part 3의 재발방지 설계는 "롤백 감지"가 아니라 **"버전·상태 가시성"**을 목표로 해야 함 |
| 2 | **F4(SCP 전용·버전관리 부재)는 여전히 유효한 위험.** 이번엔 사고가 없었을 뿐, 구조는 그대로다 → Part 3 버전관리 도입은 **계속 필요** |
| 3 | S-4의 실제 작업 = **문서의 버전 기록 정정**(v8.2 → v10.0.0-ops). 코드는 이미 단일소스(`version.py`) |
| 4 | H-G(PWA↔엔진 계약 불일치)는 실재 — `/api/assets/total` 미구현. Part 3 계약표 작성 대상 |
| 5 | **Part 1의 미지수(승인→집행 단절)가 유일한 실제 장애.** 오늘 관측성 3건을 배포했으므로 **다음 거래일 09:00 릴리스에서 로그로 규명 가능** |

---

## 부수 발견

| # | 내용 |
|---|---|
| 1 | `patch_v91_engine.py`가 서버에 잔존 — 일회성 패치 스크립트가 배포 디렉터리에 남아 `APP_VERSION` grep을 오염시킴(검증 오탐 원인). 정리 대상 |
| 2 | `block_accuracy` 기록이 **2026-06-17 이후 중단** — 기능은 살아 있으나 데이터가 안 쌓임 |
| 3 | `event_log`의 `BUY`/`BLOCK`/`SELL` 행은 `created_at`이 **NULL** — 기간 기반 분석 불가 |
