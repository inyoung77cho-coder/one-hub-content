# S30-ZC 결과 — 첫 주에 떠나지 않게 (온보딩 첫 판단 · 빈 화면 다음 걸음)

> 2026-09-05 · 선행 S30-ZB(`b917666`) · 첫 판단이 없으면 심판석·주간 리포트가 영원히 빈다.

## 계측(불변)
S26 5숫자 불변. 성능: 마운트 API index 22 · today 15 · english 9 · etf 9 · next/dynamic 7 · cachedJson 미적용 6 · 최대 4332. verify_s19 FAIL=0. webpack build 통과.

## S30-6 · 온보딩 마지막에 첫 판단
- **STEP 5(완료)에 판단 한 건** — `방금 넣은 <종목>, 지금 어떻게 보시나요? [계속 보유][줄일까 고민][관망]`. 입력한 종목 중 **가장 비중 큰(코드 있는) 하나**(`firstStock`). 누르면 `recordDecisionWithPrice({ source: "onboarding" })`(기존 공용 함수 재사용).
- ★**전제 수정** — 온보딩은 종목을 `{name, uk}` 로만 저장하고 **종목코드가 없었음**. 코드 없이는 판단을 기록할 수 없어(채점 체인이 안 붙음), `addStock` 이 `master-search` 로 **코드·현재가를 함께 캡처**(`{name, uk, code, price}`)하도록 고침. 코드를 못 잡으면(백엔드 미회신 등) 이 단계는 조용히 안 나옴(건너뜀).
- **건너뛰기** 제공(강요하면 완료율↓). **기록 직후 이유 설명** — `✓ 첫 판단이 기록됐습니다 · 3거래일 뒤 채점돼 성적표에 쌓입니다 · 월요일마다 요약`. 이 한 문장이 앱을 쓰는 이유를 설명.
- **보유 종목이 없으면(부동산만) 이 단계가 안 나옴**(`firstStock` null).
- ⚠️ `master-search` 응답의 코드 필드명을 로컬에서 확정 못 해 `code||symbol||ticker||종목코드` 로 방어적으로 잡음 — 실기기에서 코드가 실제로 잡히는지 **사용자 확인 필요**(못 잡으면 단계가 안 뜰 뿐 회귀는 없음).

## S30-7 · 빈 화면에 다음 한 걸음
| 화면 | 처리 |
|---|---|
| 심판석(판단 0건) | **이미 충족**(S24-8) — 안내 문구 + `오늘 화면에서 판단 남기기 →` 버튼 + `예시` 배지(렌더 경로 확인, line 54–77). |
| 성적표(채점 0건·판단>0) | `아직 채점된 판단이 없습니다` → **`N건 기록됨 · 첫 채점까지 3거래일`**(진행 중임을 보임). |
| 오늘 조치 0 + 보유 0 | `증권사 연동 보유 종목이 없어요` → **`보유 종목을 넣으면 매일 조치할 것을 알려드립니다 → 빠른 입력`**(클릭 시 `/pwa/input`). 두 곳(오늘 조치 행·판단 목록 빈 상태) 모두 교체. |
| 주간 리포트 미수신 | 설정 알림에 `📅 주간 리포트는 판단 기록이 쌓이면 월요일 아침에 보내드립니다`. |
- **금지 준수** — 없는 숫자를 실제처럼 채우지 않음(성적표 예시는 `예시` 배지 유지).

## 합격선 체크
- [x] 온보딩 마치면 판단 1건 기록(코드 캡처 시) · `source:"onboarding"` · 건너뛸 수 있음 · 보유 없으면 단계 없음
- [x] 판단 0건 계정 심판석에 다음 걸음 + 이동 버튼(기존 충족)
- [x] "증권사 연동" 문구가 직접 입력 사용자에게 더 이상 안 뜸(오늘 화면 2곳 교체)
- [x] 예시 수치가 실제로 오인되지 않음(`예시` 배지)
- [x] verify FAIL=0 · webpack build 통과
- ⚠️ 온보딩 코드 캡처 실동작·실기기 육안은 사용자 몫.

## 커밋
- `S30 ZC: 온보딩 첫 판단(코드 캡처) + 빈 화면 다음 걸음(증권사연동 전제 제거)`
