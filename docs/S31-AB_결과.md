# S31-AB 결과 — 은퇴·인출 축

> 2026-09-06 · 선행 S31-AA(`2eb3bfb`) · 자산 크고 지불능력 높은 층이 5일 만에 닫던 문제. 앱에 '단계'를 넣는다.

## 계측(불변)
S26 5숫자 불변. 성능: 마운트 API index 22 · today 15 · english 9 · etf 9 · next/dynamic **8**(WithdrawCard 추가) · cachedJson 미적용 6 · 최대 4332. verify_s19 FAIL=0. webpack build 통과.

## S31-4 · "이 돈으로 몇 년" (실측 PASS)
Node 실측(운용 8억·생활비 320·연금 140·배당 45만):
```
{coverRate:58, needMonthlyManwon:135, withdrawRate:2, years:49, ruleOk:true}
커버율 58%·월 인출 135만·인출률 2.0%·지속 49년·4%룰 이하 · 부족분0→years null : 모두 PASS
```
- **'단계' 개념** `onehub_life_stage`(accumulate/transition/withdraw) — 온보딩 STEP1에 질문 하나(`언제쯤 이 돈을 쓰기 시작?`) 추가, `SYNC_KEYS` 등록, **기본 accumulate(회귀 없음)**.
- **`lib/withdrawPlan.js`(신규) — 순수 산수만.** 월 인출액=생활비−(연금+배당), 연 인출률=연 인출액÷운용자산, 지속 연수=자산÷연 인출액(**수익률 0% 가정**). 예측·확률·최적화 없음.
- **가정을 결과와 같은 크기로** — `시장 수익률 0%·물가 상승 미반영 기준입니다`(경고색 박스). **4%룰은 참고선**(`연 4% 이하를 권장하는 견해가 있습니다`) — 권유 아님. 몬테카를로·성공확률 안 만듦.
- **인출 모드 오늘 화면 상단 변경** — `lifeStage==="withdraw"` 면 `WithdrawCard`(이번 달 인출 계획·지속 연수)를 맨 위(flex order 0), 조치는 그 아래. **축적기 사용자엔 안 뜸**(회귀 없음). S26 `Card` 스타일, 새 카드 아님(기존 토큰).
- **함정 준수** — 연금은 **국민연금공단 예상 수령액을 사용자가 직접 입력**(조기·연기 최적화 계산 안 함). 주택연금 범위 밖. 인출 모드에 **매일 조치 요구 안 함**(주기 월간).

## S31-5 · 생활비 커버율
- **월 현금흐름 한 줄** — `목표 생활비 320 · 연금·배당 185 (커버율 58%) · 부족분 135만 → 자산 인출`(WithdrawCard).
- **배당·이자는 사용자 입력만** — 없으면 0 + `배당 정보를 넣으면 커버율에 반영됩니다`. **추정 안 함**(S25 원칙).
- **월 1회 알림** — `todayCadence` 매월 첫 영업일 훅에 `withdraw_cover`(인출기·커버율·지속연수) 추가. 매일 아님. `withdraw=null`(축적기)이면 안 뜸.
- **세금 달력(S22-8)과 이어짐** — 인출기에게 연금·세금은 같은 이야기(11월 절세 훅 기존 유지, 커버율 훅과 같은 cadence 층).

## 합격선 체크
- [x] 온보딩에서 단계 선택 가능·기본 축적
- [x] 생활비·연금 넣으면 인출률·지속 연수(실측 PASS)
- [x] 가정 문구가 결과와 같은 크기(경고색 박스)
- [x] 인출 모드에서 오늘 화면 첫 줄이 WithdrawCard로 바뀜 · 축적기 화면 불변(회귀 없음)
- [x] 커버율이 근거 숫자와 함께 · 데이터 없는 항목 추정 안 함 · 월초 1회 알림
- [x] verify FAIL=0 · webpack build 통과
- ⚠️ 실기기 육안(온보딩 단계 저장→오늘 화면 전환·월초 훅 발동)은 로그인 사용자 몫.

## 규제 메모
- 인출 계산은 산수·가정 명시라 자문 아님. ⚠️ 유료화 직전 전문가 확인 필요(0-2).

## 커밋
- `S31 AB: 생애 단계 + 인출 계획(withdrawPlan) + 생활비 커버율(월 1회)`
